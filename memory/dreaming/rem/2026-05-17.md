# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- Android Studio конвертор — всички проекции (116 файла): `@JvmField` на instance property-та потиска getters/setters; `Projection.name` property clash-ваше с `getName()`/`setName()`; Companion object константи нуждаят `@JvmStatic` за Java; `equals()`/`hashCode()` — конверторът пра [confidence=0.58 evidence=memory/2026-05-14.md:19-22]
- Android Studio конвертор — всички проекции (116 файла): Потребителят конвертира всички проекции в `com/jhlabs/map/proj/`; ~300 грешки в началото, всички оправени; Основни fixes:; Property vs Method clash (`val isEqualArea` vs `open fun isEqualArea()`) [confidence=0.58 evidence=memory/2026-05-14.md:15-18]
- BaseApplication.kt + BaseNavigationService.kt: Fix: локални `val` или `!!`; `setInstance(this)` — generic метод, Java не може да infer-не типа; Fix: премахнах generic-а от `setInstance` [confidence=0.58 evidence=memory/2026-05-14.md:34-36]
